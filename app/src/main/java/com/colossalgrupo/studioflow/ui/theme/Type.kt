package com.colossalgrupo.studioflow.ui.theme

import androidx.compose.material3.Typography

// Uses the Material 3 default type scale with the system font (Roboto).
val StudioFlowTypography = Typography()
